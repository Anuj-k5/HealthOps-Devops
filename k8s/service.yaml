apiVersion: v1
kind: Service
metadata:
  name: healthops-service
  namespace: healthops
spec:
  selector:
    app: healthops-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
  type: LoadBalancer
